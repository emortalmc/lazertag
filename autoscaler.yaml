apiVersion: autoscaling.agones.dev/v1
kind: FleetAutoscaler
metadata:
  name: lazertag
  namespace: emortalmc

spec:
  fleetName: lazertag
  policy:
    type: Buffer
    buffer:
      bufferSize: 1
      minReplicas: 3
      maxReplicas: 5
